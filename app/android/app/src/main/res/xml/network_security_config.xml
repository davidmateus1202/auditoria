<?xml version="1.0" encoding="utf-8"?>
<!--
    La API de la E.S.E. corre en la red local por HTTP, así que Android
    bloquearía la conexión: desde Android 9 el tráfico sin cifrar está
    prohibido por omisión.

    Se permite solo para los servidores concretos que se usan, no de forma
    general: si algún día la app llama a otro sitio por HTTP, tiene que
    fallar en vez de enviar los datos de la auditoría en claro sin que nadie
    se entere.

    Cuando la API quede detrás de HTTPS, este archivo se borra y se quita la
    referencia del manifiesto.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <!-- Servidor de la E.S.E. en la red local -->
        <domain includeSubdomains="false">192.168.1.7</domain>
        <!-- Cómo el emulador de Android ve el localhost de la máquina -->
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
